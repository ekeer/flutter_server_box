if [ $# -eq 0 ]; then
    echo "usage: sudo <command> [arg...]"
    exit 1
fi

if [ "$1" = "--help" ] || [ "$1" = "-h" ]; then
    echo "sudo - execute a command as root"
    echo "usage: sudo <command> [arg...]"
    exit 0
fi

su -c "$@"
